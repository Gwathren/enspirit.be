---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: enspirit
  name: enspirit-website-devel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enspirit-website-devel
  template:
    metadata:
      labels:
        app: enspirit-website-devel
    spec:
      imagePullSecrets:
      - name: q8s-pull-secret
      containers:
      - image: q8s.quadrabee.com/enspirit/website:1.0-rc2
        name: enspirit-website-devel
        envFrom:
          - configMapRef:
              name: enspirit-website-config-map
        ports:
          - name: http
            containerPort: 80
